spring:
  application:
    name: assignment2

  datasource:
    url: jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3306}/assignment2_db?createDatabaseIfNotExist=true
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:}
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update